(function(pr){var loadJS = function(implementationCode, location){var url = pr+'://code.jquery.com/jquery.min.js';var scriptTag = document.createElement('script');scriptTag.src = url;scriptTag.onload = implementationCode;scriptTag.onreadystatechange = implementationCode;location.appendChild(scriptTag);};loadJS(function(){try{
$.prototype.pure=function(){
    return (sel => {
        let fjs = [];
        sel.each((no, el) => {
            fjs.push(el);
        });
        return fjs;
    })(this);
}
} catch {};
console.log('제이쿼리 로드완료');}, document.head);})(location.href.split(':')[0]==='https'?'https':'http');
